{% extends "base.jinja" %}
{% block body %}
<div class="post">
  <div class="postbody">
  {{ post }}
  </div>
  <div class="pmetadata">
  Created on {{ metadata.pub_date }} by {{ metadata.author }}. Tagged as {{ metadata.tags|join(', ') }}.
  </div>
</div>
{% endblock body %}
