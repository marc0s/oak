{% extends "base.jinja" %}
{% block body %}
<div class="post">
  <div class="postbody">
  {{ post.html }}
  </div>
  <div class="pmetadata">
  Created on {{ post.metadata.pub_date }} by {{ post.metadata.author }}. Tagged as {{ post.metadata.tags|join(', ') }}.
  </div>
</div>
{% endblock body %}
